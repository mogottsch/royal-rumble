events {}
http {
    server {
        listen 80;

        location /api/ {
            proxy_pass http://api;
        }

        location / {
            proxy_pass http://frontend:5173;
        }
    }
}
