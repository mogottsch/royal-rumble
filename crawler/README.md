
```bash
poetry run crawl
```
- Fetches wrestlers as defined in `years` variable in `crawl.py` and stores them in `data/matches` folder.

```bash
./get_names.sh
```
- Fetches wrestler names from `data/matches` folder and stores them in `data/name.txt`.

```bash
